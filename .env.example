# Copy this file to .env for optional local environment variables.
#
# The Gemini API key is normally stored securely in the OS keyring when you
# save it from the app, so you usually do NOT need this file.
#
# Optional: set GEMINI_API_KEY to import a key into the keyring on next startup.
GEMINI_API_KEY=
